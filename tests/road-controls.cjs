const vm=require('node:vm'),fs=require('node:fs'),assert=require('node:assert/strict');
const events={},els={};const ctx=new Proxy({createRadialGradient:()=>({addColorStop(){}})},{get:(o,k)=>o[k]||(()=>{})});
function el(id){return els[id]??=( {value:id==='drug'?'none':'0',dataset:{},getContext:()=>ctx,getBoundingClientRect:()=>({width:800,height:650}),replaceChildren(){},setPointerCapture(){}})}
let frame;const env={document:{getElementById:el,querySelectorAll:()=>[]},window:{addEventListener:(n,fn)=>events[n]=fn},ResizeObserver:class{observe(){}},devicePixelRatio:1,requestAnimationFrame:fn=>frame=fn,Math};vm.runInNewContext(fs.readFileSync(require('node:path').join(__dirname,'../route-vigilance.js'),'utf8'),env);let now=0;const tick=n=>{for(let i=0;i<n;i++)frame(now+=16)};const key=(type,code,key)=>events[type]({code,key,target:{tagName:'CANVAS'},preventDefault(){}});
els['road-start'].onclick();key('keydown','ShiftLeft','Shift');tick(120);key('keyup','ShiftLeft','Shift');assert(+els['road-canvas'].dataset.speed>35);key('keydown','ControlLeft','Control');tick(80);key('keyup','ControlLeft','Control');assert.equal(+els['road-canvas'].dataset.speed,0);
key('keydown','ShiftLeft','Shift');tick(150);events.blur();const before=els['road-canvas'].dataset.distance;tick(50);assert.equal(els['road-canvas'].dataset.distance,before);els['road-start'].onclick();tick(4000);assert.equal(els['road-canvas'].dataset.running,'false');assert(els['road-results'].textContent.includes('Bilan'));console.log('PASS: Maj gauche, Ctrl gauche, pause au blur, reprise, trajet complet et bilan');
